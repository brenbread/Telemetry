#language: python
matrix:
    include:
        - os: linux
          sudo: true
          dist: trusty
          group: edge
          install:
            - sudo apt install python-pip
            - pip install virtualenv
            - ./setup
        - os: linux
          sudo: true
          dist: trusty
          group: edge
          install:
            - sudo apt install python-pip
            - pip install virtualenv
            - ./setup-py3
        - os: osx
          install:
            - pip install virtualenv
            - ./setup
script:
  - ./telemetry &
  - sleep 2
  - wget http://localhost:5001
# after_success: